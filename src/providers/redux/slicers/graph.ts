import { createSlice } from '@reduxjs/toolkit';

const graphSlice = createSlice({
    name: "graphSlice",
    initialState: {
        
    },
    reducers: {

    }
});

export const {

} = graphSlice.reducer;

export default graphSlice;