import React, { useState } from 'react';

import { GraphNavContainer } from '../../styles/containers';


const GraphNav = () => {
    return <GraphNavContainer>

    </GraphNavContainer>
};

export default GraphNav;